---
title: Save states & replay tokens
order: 4
footnote: Games, simulators, any deterministic system.
---

A fixed-schema snapshot is a save file. Store it in a cookie, a QR code,
a text field in a form. Decoding is deterministic and version-safe, as
long as both ends share the same schema revision.
